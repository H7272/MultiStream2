*
* Stream Setting
*

TimeOut        = 5;	// SecTime to shut down the camera after All the client is gone.   
JPGQuality     = 75;	// Set the Qualit value so that the JPEG converted file size does not exceed the data buffer size.    
DataBufferSize = 65300; // SendDataBufferSize.
CameraPort     = 0;     // Connect Camea Port Number.
CameraWidth    = 640;	//
CameraHight    = 480;	//
CameraFPS      = 20;	//
UsePort	       = 5000;	// Application USE Port Number.

